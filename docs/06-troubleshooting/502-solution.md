# 502 错误解决方案

## 🔍 问题诊断

根据测试结果，**后端服务是正常的**：

```bash
# 本地访问测试
curl http://127.0.0.1:8082/api/food-diary/records
# ✅ 返回: HTTP/1.1 200

# 局域网访问测试
curl http://192.168.31.185:8082/api/food-diary/records
# ✅ 返回: HTTP/1.1 200

# 端口监听状态
netstat -an | grep 8082
# ✅ tcp46  0  0  *.8082  *.*  LISTEN
```

## ✅ 解决方案

### 步骤 1：配置微信开发者工具

**这是最重要的一步！**

1. 打开微信开发者工具
2. 点击右上角 **"详情"** 按钮
3. 选择 **"本地设置"** 标签页
4. **必须勾选**以下选项：
   - ✅ **不校验合法域名、web-view（业务域名）、TLS版本以及HTTPS证书**
   - ✅ **不校验安全域名、TLS版本以及HTTPS证书**

### 步骤 2：清除缓存并重新编译

1. 在微信开发者工具中：
   - 点击菜单栏 **"工具"** → **"清除缓存"** → **"全部清除"**

2. 重新编译项目：
   ```bash
   cd /Users/baitao/project/bty/BTYMini-uni
   npm run dev:mp-weixin
   ```

3. 在微信开发者工具中点击 **"编译"** 按钮

### 步骤 3：查看测试结果

我已经在详情页添加了自动测试功能，打开详情页时会自动测试 API 连接。

**查看测试结果**：
1. 打开微信开发者工具的 **"控制台"** 标签页
2. 进入详情页
3. 查看控制台输出的测试结果

你应该会看到类似这样的输出：

```
🔍 测试 API 连接: http://192.168.31.185:8082

📍 测试 1: 基础连接
✅ 基础连接成功: {statusCode: 200, data: {...}}

📍 测试 2: Token 检查
Token: 未设置

📍 测试 3: 抠图服务
⚠️  抠图服务未启动（可选）

✅ API 连接测试完成
```

### 步骤 4：手动测试 API

如果需要手动测试，可以在控制台运行：

```javascript
// 方法 1: 使用测试工具
import { testApiConnection } from '@/utils/test-api.js'
testApiConnection()

// 方法 2: 直接发起请求
uni.request({
  url: 'http://192.168.31.185:8082/api/food-diary/records',
  method: 'GET',
  data: { pageNo: 1, pageSize: 5 },
  success: (res) => console.log('成功:', res),
  fail: (err) => console.error('失败:', err)
})
```

## 🚨 常见问题

### 问题 1: 仍然报 502 错误

**原因**: 微信开发者工具设置未生效

**解决**:
1. 完全关闭微信开发者工具
2. 重新打开项目
3. 再次检查"详情 → 本地设置"中的选项是否勾选

### 问题 2: 网络超时

**原因**: 防火墙或网络代理阻止了连接

**解决**:
1. 关闭系统代理或 VPN
2. 检查 macOS 防火墙设置：
   - 系统偏好设置 → 安全性与隐私 → 防火墙
   - 确保允许传入连接

### 问题 3: 连接被拒绝

**原因**: 后端服务未运行

**解决**:
```bash
# 检查服务是否运行
lsof -i :8082

# 如果没有输出，说明服务未运行
# 需要在 IntelliJ IDEA 中启动后端服务
```

## 📱 真机调试

如果要在真机上调试：

### 1. 确保在同一局域网

- 手机和电脑连接同一个 WiFi
- 电脑 IP: 192.168.31.185

### 2. 微信小程序设置

- 打开微信开发者工具
- 点击"预览"或"真机调试"
- 扫码在手机上打开

### 3. 查看手机端日志

- 在微信开发者工具的"真机调试"页面
- 可以看到手机端的 console 日志

## 🔧 抠图服务

当前状态：**未启动**（端口 8090）

这不影响基础 API 功能，但会影响图片抠图功能。

如需使用抠图功能，需要启动抠图服务。

## 📊 验证清单

完成以下检查：

- [x] 后端服务（8082）正常运行
- [x] 端口监听配置正确
- [x] 局域网访问正常
- [ ] 微信开发者工具设置正确 ← **最关键**
- [ ] 已清除缓存并重新编译
- [ ] 查看控制台测试结果

## 💡 提示

1. **最重要的是微信开发者工具的设置**，确保勾选"不校验合法域名"选项
2. 修改配置后一定要**清除缓存并重新编译**
3. 查看**控制台日志**了解详细的错误信息
4. 后端服务本身是正常的，问题在于小程序端的网络请求配置

## 📞 如果还有问题

如果按照以上步骤操作后仍然有问题，请提供：

1. 微信开发者工具控制台的完整错误日志
2. Network 面板中的请求详情（Request Headers、Response）
3. 微信开发者工具的版本号

---

**更新时间**: 2026-06-21
**测试状态**: ✅ 后端服务正常
